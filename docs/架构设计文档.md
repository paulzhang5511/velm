# Velm (Vello + Elm) 详细架构设计文档 (v2.0 Zero-Glue Edition)

**项目名称**：Velm (Vello + Elm)

**全称**：Velm-UI: A Native Android 2D GUI Framework powered by Vello & The Elm Architecture

**核心定位**：基于 Rust `vello` GPU 矢量渲染引擎与 Android 原生 `NativeActivity` 构建的轻量级、声明式、**零胶水层（不依赖 `android-activity`）** 的 Android 原生 UI 框架。采用 Elm 架构（TEA）进行状态驱动，全套 API 结构体、布局模型与事件流完全对齐 Android 原生开发概念（`Activity`、`View`、`ViewGroup`、`MotionEvent`、`LayoutParams` 等）。

---

## 1. 代码组织结构与目录设计

整个框架采用高内聚、低耦合的模块化架构，源码统一存放在 `src/` 目录下。

```text
velm-demo/
├── Cargo.toml                    # Rust 2024 Edition 依赖与包配置
├── AndroidManifest.xml           # Android NDK NativeActivity 清单文件
└── src/
    ├── lib.rs                    # 开发者业务逻辑入口与 ANativeActivity_onCreate C 导出
    ├── app/                      # 应用生命周期与状态契约 (Elm Architecture)
    │   ├── mod.rs
    │   ├── activity.rs           # Activity trait 与 Intent 抽象定义
    │   └── state.rs              # 运行时状态存储
    ├── engine/                   # 零胶水层核心调度与事件循环
    │   ├── mod.rs
    │   ├── activity_thread.rs    # ANativeActivityCallbacks 绑定与 ALooper 驱动
    │   └── hit_test.rs           # 视图树 Hit-Testing 递归命中测试算法
    ├── event/                    # Android 原生事件解析
    │   ├── mod.rs
    │   └── motion_event.rs       # AInputEvent 到 MotionEvent 的封装
    ├── layout/                   # 布局与测量计算
    │   ├── mod.rs
    │   └── measure.rs            # 基于 Taffy/Flexbox 的 Measure/Layout 算法
    ├── platform/                 # 底层 NDK 平台句柄桥接
    │   ├── mod.rs
    │   └── window.rs             # ANativeWindow 到 raw-window-handle 0.5 的封装
    ├── render/                   # Vello GPU 矢量渲染管线
    │   ├── mod.rs
    │   └── vello_renderer.rs     # WGPU Surface 初始化与 View Tree Scene 绘制
    └── view/                     # 1:1 对齐 Android 原生视图体系
        ├── mod.rs
        ├── params.rs             # LayoutParams & LayoutDimension
        ├── text_view.rs          # TextView 节点抽象
        └── view_group.rs         # ViewGroup & View 枚举节点

```

---

## 2. 核心系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            1. Application Layer                             │
│       MainActivity (impl Activity) ──► View Tree (ViewGroup / TextView)     │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ View Tree & Messages
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                    2. Pure NDK Engine (ActivityThread)                      │
│  ┌─────────────────────────┐               ┌─────────────────────────────┐  │
│  │ ALooper_pollOnce        │──────────────►│ AInputQueue_getEvent        │  │
│  └────────────┬────────────┘               └──────────────┬──────────────┘  │
│               │ Window / Draw Events                      │ MotionEvent     │
│  ┌────────────▼────────────┐               ┌──────────────▼──────────────┐  │
│  │ Measure & Layout        │               │ Hit-Testing (engine/hit_test)│  │
│  └────────────┬────────────┘               └──────────────┬──────────────┘  │
│               │ Computed Layout                           │ Click/Touch Msg │
│  ┌────────────▼────────────┐                              │                 │
│  │ Vello Scene Render      │◄─────────────────────────────┘                 │
│  └────────────┬────────────┘                                                │
└───────────────┼─────────────────────────────────────────────────────────────┘
                │ Raw Window Pointer
┌───────────────▼─────────────────────────────────────────────────────────────┐
│                 3. Platform Bridge (NativeWindowWrapper)                    │
│             raw-window-handle 0.5 ──► WGPU Surface ──► Hardware             │
└─────────────────────────────────────────────────────────────────────────────┘

```

---

## 3. 项目配置文件 (`Cargo.toml`)

```toml
[package]
name = "velm-demo"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
# 核心底层 NDK 与 Window 句柄绑定（零胶水层）
raw-ndk-sys = "0.1.2"
raw-window-handle = "0.5.2"

# GPU 矢量渲染引擎与图形基元
vello = "0.2"
peniko = "0.1"

# 弹性布局引擎
taffy = "0.3"

# 系统日志与调试
log = "0.4"
android_logger = "0.13"

# 异步任务支持
pin-project = "1.1"

```

---

## 4. 详细模块代码实现

### 4.1 平台底层句柄 (`platform/window.rs`)

```rust
//! platform/window.rs - 原始 NDK ANativeWindow 封装

use std::ffi::c_void;
use std::ptr::NonNull;
use log::{error, info, trace};
use raw_ndk_sys::{
    ANativeWindow, ANativeWindow_getFormat, ANativeWindow_getHeight, ANativeWindow_getWidth,
    ANativeWindow_setBuffersGeometry, WINDOW_FORMAT_RGBA_8888,
};
use raw_window_handle::{
    AndroidDisplayHandle, AndroidNdkWindowHandle, HasRawDisplayHandle, HasRawWindowHandle,
    RawDisplayHandle, RawWindowHandle,
};

pub struct NativeWindowWrapper {
    window_ptr: NonNull<ANativeWindow>,
}

impl NativeWindowWrapper {
    pub unsafe fn from_raw_ndk_ptr(ptr: *mut ANativeWindow) -> Option<Self> {
        info!("[NativeWindowWrapper] Validating raw ANativeWindow pointer: {:p}", ptr);
        let window_ptr = NonNull::new(ptr)?;
        Some(Self { window_ptr })
    }

    pub fn as_raw_ptr(&self) -> *mut ANativeWindow {
        self.window_ptr.as_ptr()
    }

    pub fn configure_buffers(&self, width: i32, height: i32) -> Result<(), i32> {
        let raw_ptr = self.as_raw_ptr();
        info!("[NativeWindowWrapper] Config geometry: {}x{}, Format: RGBA_8888", width, height);

        unsafe {
            let result = ANativeWindow_setBuffersGeometry(
                raw_ptr,
                width,
                height,
                WINDOW_FORMAT_RGBA_8888 as i32,
            );

            if result == 0 {
                Ok(())
            } else {
                error!("[NativeWindowWrapper] Config failed. Code: {}", result);
                Err(result)
            }
        }
    }

    pub fn log_attributes(&self) {
        let raw_ptr = self.as_raw_ptr();
        unsafe {
            let width = ANativeWindow_getWidth(raw_ptr);
            let height = ANativeWindow_getHeight(raw_ptr);
            let format = ANativeWindow_getFormat(raw_ptr);
            info!("[NativeWindowWrapper] Size: {}x{}, Format: {}", width, height, format);
        }
    }
}

impl HasRawWindowHandle for NativeWindowWrapper {
    fn raw_window_handle(&self) -> RawWindowHandle {
        trace!("[NativeWindowWrapper] Exporting RawWindowHandle::AndroidNdk");
        let mut handle = AndroidNdkWindowHandle::empty();
        handle.a_native_window = self.window_ptr.as_ptr() as *mut c_void;
        RawWindowHandle::AndroidNdk(handle)
    }
}

impl HasRawDisplayHandle for NativeWindowWrapper {
    fn raw_display_handle(&self) -> RawDisplayHandle {
        RawDisplayHandle::Android(AndroidDisplayHandle::empty())
    }
}

```

---

### 4.2 View 体系抽象 (`view/mod.rs`)

```rust
//! view/mod.rs - Android 风格 UI 视图结构定义

use peniko::Color;

#[derive(Clone, Copy, Debug, PartialEq)]
pub enum LayoutDimension {
    MatchParent,
    WrapContent,
    Dp(f32),
}

#[derive(Clone, Debug)]
pub struct LayoutParams {
    pub width: LayoutDimension,
    pub height: LayoutDimension,
    pub margin_top: f32,
    pub margin_bottom: f32,
    pub margin_left: f32,
    pub margin_right: f32,
}

impl Default for LayoutParams {
    fn default() -> Self {
        Self {
            width: LayoutDimension::WrapContent,
            height: LayoutDimension::WrapContent,
            margin_top: 0.0,
            margin_bottom: 0.0,
            margin_left: 0.0,
            margin_right: 0.0,
        }
    }
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Orientation {
    Horizontal,
    Vertical,
}

/// 计算得出的 View 绘制 Layout 区域 (对应 Frame/Bounds)
#[derive(Clone, Copy, Debug, Default)]
pub struct Rect {
    pub x: f32,
    pub y: f32,
    pub width: f32,
    pub height: f32,
}

impl Rect {
    pub fn contains(&self, px: f32, py: f32) -> bool {
        px >= self.x && px <= (self.x + self.width) && py >= self.y && py <= (self.y + self.height)
    }
}

pub struct TextView<Msg> {
    pub text: String,
    pub text_size: f32,
    pub text_color: Color,
    pub layout_params: LayoutParams,
    pub on_click_listener: Option<Msg>,
    pub computed_rect: Rect,
}

pub struct ViewGroup<Msg> {
    pub orientation: Orientation,
    pub layout_params: LayoutParams,
    pub children: Vec<View<Msg>>,
    pub on_click_listener: Option<Msg>,
    pub computed_rect: Rect,
}

pub enum View<Msg> {
    TextView(TextView<Msg>),
    ViewGroup(ViewGroup<Msg>),
}

impl<Msg: Clone> View<Msg> {
    pub fn linear_layout(orientation: Orientation, children: Vec<View<Msg>>) -> Self {
        View::ViewGroup(ViewGroup {
            orientation,
            layout_params: LayoutParams {
                width: LayoutDimension::MatchParent,
                height: LayoutDimension::MatchParent,
                ..Default::default()
            },
            children,
            on_click_listener: None,
            computed_rect: Rect::default(),
        })
    }

    pub fn text_view(text: impl Into<String>) -> Self {
        View::TextView(TextView {
            text: text.into(),
            text_size: 16.0,
            text_color: Color::WHITE,
            layout_params: LayoutParams::default(),
            on_click_listener: None,
            computed_rect: Rect::default(),
        })
    }

    pub fn set_text_size(mut self, size: f32) -> Self {
        if let View::TextView(ref mut tv) = self {
            tv.text_size = size;
        }
        self
    }

    pub fn set_text_color(mut self, color: Color) -> Self {
        if let View::TextView(ref mut tv) = self {
            tv.text_color = color;
        }
        self
    }

    pub fn set_on_click_listener(mut self, msg: Msg) -> Self {
        match self {
            View::TextView(ref mut tv) => tv.on_click_listener = Some(msg),
            View::ViewGroup(ref mut vg) => vg.on_click_listener = Some(msg),
        }
        self
    }
}

```

---

### 4.3 布局测量算法 (`layout/measure.rs`)

使用 Flexbox 布局模型将声明式的 `View` 树测量并映射到具体的点与物理坐标上：

```rust
//! layout/measure.rs - View 树测量与布局计算算法

use crate::view::{LayoutDimension, Orientation, Rect, View};

/// 递归计算 View 树每个节点的绝对尺寸与物理坐标
pub fn measure_and_layout<Msg>(root: &mut View<Msg>, screen_width: f32, screen_height: f32) {
    layout_node(root, 0.0, 0.0, screen_width, screen_height);
}

fn layout_node<Msg>(
    node: &mut View<Msg>,
    x: f32,
    y: f32,
    parent_width: f32,
    parent_height: f32,
) {
    match node {
        View::TextView(tv) => {
            let width = match tv.layout_params.width {
                LayoutDimension::MatchParent => parent_width,
                LayoutDimension::Dp(val) => val,
                LayoutDimension::WrapContent => (tv.text.len() as f32 * tv.text_size * 0.6).min(parent_width),
            };
            let height = match tv.layout_params.height {
                LayoutDimension::MatchParent => parent_height,
                LayoutDimension::Dp(val) => val,
                LayoutDimension::WrapContent => tv.text_size * 1.4,
            };

            tv.computed_rect = Rect { x, y, width, height };
        }
        View::ViewGroup(vg) => {
            let width = match vg.layout_params.width {
                LayoutDimension::MatchParent => parent_width,
                LayoutDimension::Dp(val) => val,
                LayoutDimension::WrapContent => parent_width,
            };
            let height = match vg.layout_params.height {
                LayoutDimension::MatchParent => parent_height,
                LayoutDimension::Dp(val) => val,
                LayoutDimension::WrapContent => parent_height,
            };

            vg.computed_rect = Rect { x, y, width, height };

            // 依据 Orientation 递归计算子视图算法
            let mut current_x = x;
            let mut current_y = y;

            for child in &mut vg.children {
                layout_node(child, current_x, current_y, width, height);

                let child_rect = match child {
                    View::TextView(c) => c.computed_rect,
                    View::ViewGroup(c) => c.computed_rect,
                };

                match vg.orientation {
                    Orientation::Vertical => current_y += child_rect.height,
                    Orientation::Horizontal => current_x += child_rect.width,
                }
            }
        }
    }
}

```

---

### 4.4 点击与触控测试算法 (`engine/hit_test.rs`)

对齐 Android 的 Touch Target 深度优先寻找算法（DFS）：

```rust
//! engine/hit_test.rs - 点击测试 (Hit-Testing) 算法

use crate::view::View;

/// 在 View 树中递归自上而下检索被击中的视图节点并返回对应的 Message
pub fn perform_hit_test<Msg: Clone>(root: &View<Msg>, x: f32, y: f32) -> Option<Msg> {
    match root {
        View::TextView(tv) => {
            if tv.computed_rect.contains(x, y) {
                tv.on_click_listener.clone()
            } else {
                None
            }
        }
        View::ViewGroup(vg) => {
            if !vg.computed_rect.contains(x, y) {
                return None;
            }

            // 反向迭代 (优先响应重叠在最上层的子视图)
            for child in vg.children.iter().rev() {
                if let Some(msg) = perform_hit_test(child, x, y) {
                    return Some(msg);
                }
            }

            // 子视图未捕获，则响应容器本身的点击事件
            vg.on_click_listener.clone()
        }
    }
}

```

---

### 4.5 触控事件解析 (`event/motion_event.rs`)

```rust
//! event/motion_event.rs - Android 风格触控事件封装

use raw_ndk_sys::{
    AInputEvent, AMOTION_EVENT_ACTION_CANCEL, AMOTION_EVENT_ACTION_DOWN, AMOTION_EVENT_ACTION_MOVE,
    AMOTION_EVENT_ACTION_UP, AMotionEvent_getAction, AMotionEvent_getX, AMotionEvent_getY,
    AINPUT_EVENT_TYPE_MOTION,
};

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum TouchAction {
    ActionDown,
    ActionMove,
    ActionUp,
    ActionCancel,
}

#[derive(Clone, Debug)]
pub struct MotionEvent {
    pub action: TouchAction,
    pub x: f32,
    pub y: f32,
}

impl MotionEvent {
    pub unsafe fn from_ndk_input_event(event: *const AInputEvent) -> Option<Self> {
        let event_type = unsafe { raw_ndk_sys::AInputEvent_getType(event) };
        if event_type != AINPUT_EVENT_TYPE_MOTION as i32 {
            return None;
        }

        let raw_action = unsafe { AMotionEvent_getAction(event) } & 0xff;
        let action = match raw_action as u32 {
            AMOTION_EVENT_ACTION_DOWN => TouchAction::ActionDown,
            AMOTION_EVENT_ACTION_MOVE => TouchAction::ActionMove,
            AMOTION_EVENT_ACTION_UP => TouchAction::ActionUp,
            AMOTION_EVENT_ACTION_CANCEL => TouchAction::ActionCancel,
            _ => return None,
        };

        let x = unsafe { AMotionEvent_getX(event, 0) };
        let y = unsafe { AMotionEvent_getY(event, 0) };

        Some(Self { action, x, y })
    }

    pub fn get_action(&self) -> TouchAction { self.action }
    pub fn get_x(&self) -> f32 { self.x }
    pub fn get_y(&self) -> f32 { self.y }
}

```

---

### 4.6 应用契约定义 (`app/activity.rs`)

```rust
//! app/activity.rs - Activity Trait 与生命周期

use crate::event::motion_event::MotionEvent;
use crate::view::View;

pub struct Intent<Message> {
    pub(crate) _tasks: Vec<()>,
    _phantom: std::marker::PhantomData<Message>,
}

impl<Message> Intent<Message> {
    pub fn none() -> Self {
        Self {
            _tasks: Vec::new(),
            _phantom: std::marker::PhantomData,
        }
    }
}

pub trait Activity: Sized + 'static {
    type Message: Send + Clone + 'static;
    type SavedInstanceState;

    fn on_create(saved_state: Option<Self::SavedInstanceState>) -> (Self, Intent<Self::Message>);
    fn update(&mut self, message: Self::Message) -> Intent<Self::Message>;
    fn on_draw(&self) -> View<Self::Message>;
    fn on_touch_event(&mut self, _event: &MotionEvent) -> Option<Self::Message> { None }
}

```

---

### 4.7 引擎核心与零胶水事件循环 (`engine/activity_thread.rs`)

```rust
//! engine/activity_thread.rs - 零胶水层事件循环引擎

use std::collections::VecDeque;
use std::ffi::c_void;
use std::ptr;

use log::info;
use raw_ndk_sys::{
    AInputQueue, AInputQueue_finishEvent, AInputQueue_getEvent, AInputQueue_preDispatchEvent,
    ALooper_pollOnce, ANativeActivity, ANativeWindow, ANativeWindow_getHeight,
    ANativeWindow_getWidth,
};

use crate::app::Activity;
use crate::engine::hit_test::perform_hit_test;
use crate::event::motion_event::{MotionEvent, TouchAction};
use crate::layout::measure::measure_and_layout;
use crate::platform::NativeWindowWrapper;

struct AppContext<A: Activity> {
    activity_instance: A,
    message_queue: VecDeque<A::Message>,
    window_wrapper: Option<NativeWindowWrapper>,
    input_queue: *mut AInputQueue,
    is_running: bool,
}

pub unsafe fn run_native_activity<A: Activity>(
    activity: *mut ANativeActivity,
    _saved_state: *mut c_void,
    _saved_state_size: usize,
) {
    android_logger::init_once(
        android_logger::Config::default()
            .with_max_level(log::LevelFilter::Trace)
            .with_tag("VelmEngine"),
    );

    info!("[ActivityThread] Executing ANativeActivity_onCreate...");

    let (activity_instance, _intent) = A::on_create(None);

    let context = Box::new(AppContext {
        activity_instance,
        message_queue: VecDeque::new(),
        window_wrapper: None,
        input_queue: ptr::null_mut(),
        is_running: true,
    });

    let context_ptr = Box::into_raw(context) as *mut c_void;

    unsafe {
        (*activity).instance = context_ptr;
        let callbacks = (*activity).callbacks;

        (*callbacks).onNativeWindowCreated = Some(on_native_window_created::<A>);
        (*callbacks).onNativeWindowDestroyed = Some(on_native_window_destroyed::<A>);
        (*callbacks).onInputQueueCreated = Some(on_input_queue_created::<A>);
        (*callbacks).onInputQueueDestroyed = Some(on_input_queue_destroyed::<A>);
        (*callbacks).onDestroy = Some(on_destroy::<A>);
    }

    info!("[ActivityThread] NativeActivity callbacks successfully bound.");
}

unsafe extern "C" fn on_native_window_created<A: Activity>(
    activity: *mut ANativeActivity,
    window: *mut ANativeWindow,
) {
    info!("[ActivityThread] Callback: onNativeWindowCreated");
    let ctx = unsafe { &mut *((*activity).instance as *mut AppContext<A>) };

    if let Some(wrapper) = unsafe { NativeWindowWrapper::from_raw_ndk_ptr(window) } {
        let width = unsafe { ANativeWindow_getWidth(window) };
        let height = unsafe { ANativeWindow_getHeight(window) };
        let _ = wrapper.configure_buffers(width, height);
        ctx.window_wrapper = Some(wrapper);

        // 初始化视图树布局
        let mut root_view = ctx.activity_instance.on_draw();
        measure_and_layout(&mut root_view, width as f32, height as f32);
    }
}

unsafe extern "C" fn on_native_window_destroyed<A: Activity>(
    activity: *mut ANativeActivity,
    _window: *mut ANativeWindow,
) {
    let ctx = unsafe { &mut *((*activity).instance as *mut AppContext<A>) };
    ctx.window_wrapper = None;
}

unsafe extern "C" fn on_input_queue_created<A: Activity>(
    activity: *mut ANativeActivity,
    queue: *mut AInputQueue,
) {
    let ctx = unsafe { &mut *((*activity).instance as *mut AppContext<A>) };
    ctx.input_queue = queue;

    unsafe { drive_event_loop(ctx) };
}

unsafe extern "C" fn on_input_queue_destroyed<A: Activity>(
    activity: *mut ANativeActivity,
    _queue: *mut AInputQueue,
) {
    let ctx = unsafe { &mut *((*activity).instance as *mut AppContext<A>) };
    ctx.input_queue = ptr::null_mut();
}

unsafe extern "C" fn on_destroy<A: Activity>(activity: *mut ANativeActivity) {
    let context_ptr = unsafe { (*activity).instance as *mut AppContext<A> };
    if !context_ptr.is_null() {
        let mut ctx = unsafe { Box::from_raw(context_ptr) };
        ctx.is_running = false;
    }
}

unsafe fn drive_event_loop<A: Activity>(ctx: &mut AppContext<A>) {
    info!("[ActivityThread] Running ALooper event loop...");

    while ctx.is_running && !ctx.input_queue.is_null() {
        let mut event = ptr::null_mut();

        // 1. 处理底层输入事件流
        while unsafe { AInputQueue_getEvent(ctx.input_queue, &mut event) } >= 0 {
            if unsafe { AInputQueue_preDispatchEvent(ctx.input_queue, event) } == 0 {
                if let Some(motion_event) = unsafe { MotionEvent::from_ndk_input_event(event) } {
                    // 自定义 Activity 拦截处理
                    if let Some(msg) = ctx.activity_instance.on_touch_event(&motion_event) {
                        ctx.message_queue.push_back(msg);
                    } else if motion_event.get_action() == TouchAction::ActionDown {
                        // 触发系统默认 Hit-Testing 点击分发机制
                        let mut root_view = ctx.activity_instance.on_draw();
                        if let Some(ref wrapper) = ctx.window_wrapper {
                            let raw_ptr = wrapper.as_raw_ptr();
                            let w = unsafe { ANativeWindow_getWidth(raw_ptr) } as f32;
                            let h = unsafe { ANativeWindow_getHeight(raw_ptr) } as f32;
                            measure_and_layout(&mut root_view, w, h);

                            if let Some(msg) = perform_hit_test(&root_view, motion_event.get_x(), motion_event.get_y()) {
                                ctx.message_queue.push_back(msg);
                            }
                        }
                    }
                }
                unsafe { AInputQueue_finishEvent(ctx.input_queue, event, 1) };
            }
        }

        // 2. 消费 Message 消息并更新应用程序状态
        let mut needs_draw = false;
        while let Some(msg) = ctx.message_queue.pop_front() {
            let _intent = ctx.activity_instance.update(msg);
            needs_draw = true;
        }

        // 3. 重绘视图树
        if needs_draw && ctx.window_wrapper.is_some() {
            let mut root_view = ctx.activity_instance.on_draw();
            if let Some(ref wrapper) = ctx.window_wrapper {
                let raw_ptr = wrapper.as_raw_ptr();
                let w = unsafe { ANativeWindow_getWidth(raw_ptr) } as f32;
                let h = unsafe { ANativeWindow_getHeight(raw_ptr) } as f32;
                measure_and_layout(&mut root_view, w, h);
            }
        }

        // 4. 挂起轮询等待唤醒 (16ms 锁定帧率)
        unsafe { ALooper_pollOnce(16, ptr::null_mut(), ptr::null_mut(), ptr::null_mut()) };
    }
}

```

---

### 4.8 应用程序入口与 C-ABI 导出 (`lib.rs`)

```rust
//! lib.rs - Velm 应用入口点

pub mod app;
pub mod engine;
pub mod event;
pub mod layout;
pub mod platform;
pub mod view;

use peniko::Color;
use app::{Activity, Intent};
use event::motion_event::MotionEvent;
use view::{Orientation, View};

pub struct MainActivity {
    counter: i32,
}

#[derive(Clone, Debug)]
pub enum MainMessage {
    ClickIncrement,
    ClickDecrement,
}

impl Activity for MainActivity {
    type Message = MainMessage;
    type SavedInstanceState = ();

    fn on_create(_saved_state: Option<()>) -> (Self, Intent<Self::Message>) {
        (MainActivity { counter: 0 }, Intent::none())
    }

    fn update(&mut self, message: Self::Message) -> Intent<Self::Message> {
        match message {
            MainMessage::ClickIncrement => self.counter += 1,
            MainMessage::ClickDecrement => self.counter -= 1,
        }
        Intent::none()
    }

    fn on_draw(&self) -> View<Self::Message> {
        View::linear_layout(
            Orientation::Vertical,
            vec![
                View::text_view(format!("Current Count: {}", self.counter))
                    .set_text_size(28.0)
                    .set_text_color(Color::WHITE),
                View::text_view("Button: +1")
                    .set_text_size(20.0)
                    .set_text_color(Color::GREEN)
                    .set_on_click_listener(MainMessage::ClickIncrement),
                View::text_view("Button: -1")
                    .set_text_size(20.0)
                    .set_text_color(Color::RED)
                    .set_on_click_listener(MainMessage::ClickDecrement),
            ],
        )
    }

    fn on_touch_event(&mut self, _event: &MotionEvent) -> Option<Self::Message> {
        None
    }
}

/// 导出 C-ABI 函数 ANativeActivity_onCreate（Rust 2024 Edition 规范）
#[no_mangle]
pub unsafe extern "C" fn ANativeActivity_onCreate(
    activity: *mut raw_ndk_sys::ANativeActivity,
    saved_state: *mut std::ffi::c_void,
    saved_state_size: usize,
) {
    assert!(!activity.is_null(), "[Velm] Received NULL ANativeActivity pointer!");

    unsafe {
        engine::run_native_activity::<MainActivity>(
            activity,
            saved_state,
            saved_state_size,
        );
    }
}

```