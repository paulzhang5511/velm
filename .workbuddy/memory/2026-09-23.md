# 2026-09-23 工作日志

## 进度

T13（输入闭环）完成，T1~T13 全部完工。
- host 单测 100（96 velm + 4 counter）；host / aarch64 / x86_64 三目标 clippy `-D warnings` 全绿；fmt 干净；android 双 target build 通过
- T11/T12/T13 串成完整引擎：counter `Activity` → 引擎出帧 → 输入 hit-test → `update` → 合并重绘

## T13 关键设计

- 引擎主循环顺序：**控制消息 → `pollOnce` → `drain_input` → `runtime.drain` 合并重绘 → 出帧**（§8.2）；一帧只 `on_draw` 一次（§7.7 禁止一触双重建）
- `drain_input`（android-only）：每条事件恰好 `finishEvent` 一次；`preDispatch != 0` 放弃（IME 接管、不 finish）；非 motion 事件 `handled=0` 交回框架
- `handle_motion`：① `on_touch_event` 拦截优先（`Some` 入队并跳过 hit-test，FR-I2）② 仅 `ActionDown` 命中一次（FR-I1/I3，按住不连发）③ 复用缓存 `frame` 做 hit-test，绝不重调 `on_draw` ④ 命中 `enqueue` / 空白不重绘（FR-I4）
- 坐标系：`MotionEvent` 物理像素 xy 与 `computed_rect` 同坐标系（ADR-12）

## 待设备验证（无设备，需用户）

- Checkpoint C：真机静态画面（启动见 +1/-1 计数、旋转/回前台恢复）
- Checkpoint D：SC-3 点 +1/-1 连点 20 次准确；SC-4 空白无反应/按住不连发
- T12 启停 20 次压测：销毁回调执行、线程 join、无 UAF/ANR

## 下一步

T14 生命周期压测与缺陷修复（`scripts/stress_lifecycle.sh`：Home/返回/回前台/旋转/连续启停 100 次）→ T15 打包配置与构建文档 → T16 质量门禁与文档归位

## T14/T15 续作（脚本与文档，无设备）

- T14 交付 `scripts/stress_lifecycle.sh`：循环启停(默认20/`--cycles`100)、Home↔前台、旋转(`wm rotation`)、可选 `--tap`；统计 `同步 ack 已收到`/`引擎线程已 join`，grep FATAL/SIGSEGV/abort/ANR；落盘 stress_lifecycle.log。旋转因 configChanges 不重建 Activity，SC-5 天然满足。
- T15 交付 `scripts/build_run.sh` + 根 `README.md`（工具链+构建/安装/日志/压测命令链）。`AndroidManifest.xml` 由 cargo-apk2 自动生成，无需外置。
- 本机无 adb/cargo-apk2/设备，实跑待用户连机。包名 `rust.counter`、ACT `android.app.NativeActivity`、tag `VelmEngine`。
- 下一步 T16：质量门禁（rustdoc/覆盖率）+ 文档归位（SC-1~9 证据归档，待设备取证）。
