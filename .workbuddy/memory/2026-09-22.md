# 2026-09-22

## T4 view 模块完成（Phase 1 逻辑泳道首个任务）

- `peniko 0.6` 从 android-only 依赖提升为通用 `[dependencies]`（SPEC §10.2：view/layout/hit_test 必须 host 可编译）。
- 新增 `crates/velm/src/view/{mod,params,text_view,view_group}.rs` + `crates/velm/tests/view.rs`；`lib.rs` 无条件 `pub mod view`。
- 13 个 host 单测全绿；host + aarch64-linux-android + x86_64-linux-android 三目标 build/clippy `-D warnings` 全绿；`cargo fmt --check` 干净。
- SPEC §7.2 回写两处：新增 `.set_layout_params(LayoutParams)`；no-op 误用只记 `log::trace!`、不启用 `debug_assert`。

### 关键技术事实（后续任务会用到）

- `peniko 0.6` 没有根级 `Color` 定义，实为 `pub type Color = color::AlphaColor<color::Srgb>`。
- `AlphaColor` 只派生 `Clone/Copy/Debug`，**没有 `PartialEq`** → `Background` 不得派生 `PartialEq`；颜色比对一律用 `to_rgba8()`（`Rgba8` 有 `PartialEq/Eq`）。
- `Color::from_rgba8(r,g,b,a)` 是 const fn，可用于 `const` 常量。

### 构建环境注意事项

- 本机 `cargo` 不在默认 PATH，需 `export PATH="$HOME/.cargo/bin:$PATH"`。
- android 构建需 `export ANDROID_NDK_ROOT="/Users/paulzhang/Library/Android/sdk/ndk/27.3.13750724"`（否则 counter 的 build.rs panic）。

## T5 event 模块完成（触控事件解码）

- 新增 `crates/velm/src/event/{mod,motion_event}.rs` + `crates/velm/tests/event.rs`；`lib.rs` 无条件 `pub mod event`。
- 8 个 host 单测全绿（累计 21）；三目标 clippy `-D warnings` + fmt 全绿。
- 纯函数 `decode_action(u32) -> Option<TouchAction>` 内部先 `& 0xff` 再匹配（幂等）；`from_ndk` 为 android-only FFI 薄壳。

### 关键技术事实（补充）

- raw-ndk-sys 常量类型：`AMOTION_EVENT_ACTION_*` 为 **u32**（`_bindgen_ty_23 = c_uint`），`AINPUT_EVENT_TYPE_*` 也是 u32（`_bindgen_ty_20`）；`AInputEvent_getType` / `AMotionEvent_getAction` 返回 **i32**，需显式 `as i32` / `as u32` 转换。
- 手写常量与 NDK 绑定的漂移防护：`#[cfg(target_os = "android")] const _: () = { assert!(...) }` —— android 编译期求值，host 因 raw-ndk-sys 是 android-only 依赖而跳过。这个模式可复用到任何「host 手写常量 ↔ NDK 绑定」场景。

### 工具提示

- `cargo/registry` 下的 `raw-ndk-sys` bindings.rs（11697 行）用 bash `grep` 搜不到内容（疑似非 UTF-8 / 超长行），改用 Grep 工具（ripgrep）可正常命中。

## T6 layout 模块完成（手写 LinearLayout 测量）

- 新增 `crates/velm/src/layout/{mod,measure}.rs` + `crates/velm/tests/layout.rs`；`lib.rs` 无条件 `pub mod layout`。
- 16 个 host 单测全绿（累计 37）；三目标 clippy `-D warnings` + fmt 全绿。
- 修正 docs 两个缺陷：容器 WrapContent = 子节点「主轴求和 / 交叉轴取最大」（非 MatchParent）；margin 四向既偏移位置也扣减可用尺寸。

### 算法要点（T7/T11/T13 会依赖）

- 两遍 O(n)：`measure` 求宽高写 `computed_rect`（x/y 置 0）→ `place` 写绝对坐标。容器 WrapContent 依赖子节点尺寸，故求尺寸必须先于定位；两遍各遍历一次全树。
- 关键坐标约定：`place` 的 `content_x/content_y` 是**父节点内容区原点**，子节点 rect = 父原点 + 主轴游标 + 自身 margin。v1 无 padding。
- 文本度量隔离为 `pub fn estimate_text_width(text, text_size_px)`（chars * size * 0.6），P1 换 skrifa advance 时只改这一处。
- 根节点可用区 = 整窗尺寸 - 根自身 margin（否则 MatchParent 根带 margin 会溢出）。
- v1 限制：WrapContent 容器内的 MatchParent 子节点按上界一次求值，不实现 Android 二次 measure。

## T7 hit_test 模块完成（DFS 命中测试）

- 新增 `crates/velm/src/engine/hit_test.rs` + `crates/velm/tests/hit_test.rs`；`lib.rs` 改无条件 `pub mod engine`，`engine/mod.rs` 内只对 `activity_thread` 子模块 gate android（否则 hit_test 失去 host 可测性）。
- 15 个 host 单测全绿（累计 54 = T4 13 + T5 8 + T6 18 + T7 15）；三目标 clippy `-D warnings` + fmt 全绿。

### 语义决策（SPEC §7.5 已回写）

- 容器「回落」发生在子节点 `find_map` 之内：子节点及后代都不产生消息时继续探测更下层兄弟 → **未绑监听的容器对点击透明**，绑了监听的容器才拦下点击。两条回归用例钉住穿透/拦截。

### 教训（通用，值得复用）

- **density 类 bug 只在 density≠1 显现**：T7 的端到端用例（layout + hit_test 串联）暴露 T6 缺陷——margin 未乘 density，三处消费点（位置偏移 / 可用区扣减 / 主轴推进量）把 dp 当像素用。T6 单测全用 density=1，故完全没暴露。已统一收敛为 `margin_px(margin, density)`，并补两条 density=2 回归。
- **结论**：凡是 dp↔px 混用的模块，回归用例必须包含 density≠1 分支；跨模块端到端用例（layout→hit_test）比单模块用例更容易抓到这类单位错误。

## T8 app 契约 + 引擎状态机完成（Phase 1 收尾）

- 新增 `app/{mod,activity,state}.rs`、`engine/events.rs`、`tests/{state_machine,app}.rs`；`lib.rs` 加 `pub mod app`。
- 27 个新 host 单测（state_machine 17 + app 10），累计 **81**；host/aarch64/x86_64 三目标 clippy `-D warnings` + fmt 全绿；android 双 target build 通过。

### 结构决策（T12 接线时按此引用）

- 生命周期状态机在 **`engine::events`**（`EngineEvent` / `EngineAction` / `EngineState` / `step`），**不在** PLAN 原写的 `app::state`（那里放 `ActivityRuntime`）。理由：状态机管窗口/队列生命周期，属引擎层；放 app 会让 T12 写出 `use crate::app::state::EngineState` 的逆分层引用。
- 重绘用**标志位** `EngineState::take_draw_request()` 表达，没有 `EngineAction::Redraw`。窗口销毁时清除未消费标志。
- `WindowResized` 不携带 density（T3 回调只给宽高）→ 沿用旧值，禁止回落 1.0。
- `Quit` 先 `DestroySurface` → `DetachQueue` → `Exit`；置位后所有事件返回空动作集。
- `ActivityRuntime`：enqueue 与 drain 分离（保证一帧只因消息重建一次视图树，§7.7）；`on_touch_event` 返回 true = 已拦截，引擎跳过 hit-test。

### 踩坑

- `Intent<Message>` 的 Clone/Copy/Default/Debug 必须**手写**：derive 会给 `Message` 加约束，而 Intent 只有 `PhantomData`。
- 手写 Clone 返回 `Self::none()` 会触发 clippy `non_canonical_clone_impl`，应返回 `*self`。
- `EngineEvent` 含 `Viewport{density: f32}` → 只能派生 `PartialEq`，不能 `Eq`。

### Checkpoint B 自检

- 已达标：全 workspace host 测试 81 全绿；fmt/clippy 干净；逻辑泳道 grep `raw_ndk|ANativeWindow|AInputQueue|ALooper|AInputEvent|extern "C"` 零命中（FFI 仅在 activity_thread.rs）。
- 待补：覆盖率 ≥85% 行需 `cargo-llvm-cov`（本机未预装，已触发安装）。

## T9 platform/window 完成（平台泳道开工）

- 新增 `platform/{mod,window}.rs` + `tests/platform.rs`；`lib.rs` 加 `pub mod platform`。5 个 host 单测（累计 86）；三目标 clippy + android 双 target build 全绿。
- `platform/mod.rs`（host 可见）：`ScreenConfig` + `density_from_dpi`（160/ANY 65534/NONE 65535 手写常量 + android const assert）；`platform/window.rs`（android-only）：`NativeWindowWrapper`（NonNull + owned 标志，from_ndk acquire / Drop release，size/format/configure_buffers，rwh 0.6 HasWindowHandle + HasDisplayHandle）。

### 关键约定（T10/T12 必须遵守）

- **T12 重写回调时必须删掉 T3 spike 在 `native_window_created` 里的 `ANativeWindow_acquire`** —— 包装器自己 acquire/release，双重 acquire 会让窗口永不释放。
- 包装类型**不实现 Send/Sync**：窗口只在引擎线程构造/使用/释放，跨线程只传裸指针。
- gate 约定同 `engine`：模块 host 可见、FFI 子模块单独 gate，别整块 `#[cfg(target_os="android")]`。

### NDK 绑定类型坑

- `ANativeWindow_LegacyFormat_WINDOW_FORMAT_RGBA_8888` 是 **u32**，而 `ANativeWindow_setBuffersGeometry` 形参是 **i32** → 需 `as i32`（值为 1，无损）。
- `ACONFIGURATION_DENSITY_*` 也是 u32（`_bindgen_ty_6`）。

### 进度

T1~T9 已完成（CP-A 仍待人类评审放行；覆盖率门槛待 cargo-llvm-cov 实测）。下一步：T10 render/vello_renderer（正式版，替换 T2 spike 的 render/mod.rs）→ T11 汇合静态画面。
